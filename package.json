{
  "name": "intisari-clips-landing",
  "version": "1.0.0",
  "description": "Landing Page & Midtrans Checkout untuk Intisari Clips",
  "private": true,
  "scripts": {
    "build:css": "npx tailwindcss -i ./src/input.css -o ./public/assets/css/tailwind.css --minify",
    "dev": "npm run build:css && wrangler pages dev public",
    "deploy": "npm run build:css && wrangler pages deploy public"
  },
  "devDependencies": {
    "sharp": "^0.35.2",
    "tailwindcss": "^3.4.19",
    "wrangler": "^3.0.0"
  }
}
